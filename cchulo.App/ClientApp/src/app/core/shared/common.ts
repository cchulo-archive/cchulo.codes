/**
 * This type will be used in client-window.service to
 * notify the UI what the current window width is
 */
export enum EWindow {
    xs = 0,
    sm = 1,
    md = 2,
    lg = 3,
    xl = 4
}

export enum ETheme {
    dark = 'dark',
    light = 'light'
}
