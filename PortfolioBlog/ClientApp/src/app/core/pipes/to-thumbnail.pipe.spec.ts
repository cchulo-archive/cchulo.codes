import { ToThumbnailPipe } from './to-thumbnail.pipe';

describe('ToThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new ToThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
